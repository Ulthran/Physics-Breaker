//
//  PhysicsObject.swift
//  Physics Breaker
//
//  Created by Charlie Bushman on 1/12/16.
//  Copyright © 2016 imaginary inc. All rights reserved.
//

import Foundation
import SpriteKit

class PhysicsObject: SKSpriteNode{
    enum Type{
        static let mass
    }
    
    var objectType: Type
    
    init(texture: SKTexture!, color: UIColor!, size: CGSize){
        super.init(texture: texture, color: color, size: size)
    }
    
    convinience init(imageName: String!, currentPiece: Type)
    {
        self.objectType = currentPiece
        let color = UIColor.blackColor()
        let texture = SKTexture(imageNamed: imageName)
        let size = CGSize()
        super.init(texture: texture, color: color, size: size)
    }
}
